public class MyFunction {
    public static float runFunction(float x_val){
        //return (float)Math.pow(Math.E,x_val)/x_val;

        return 2 *x_val;    //the default function is set to y = 2x, feel free to change this to another function of your choice.
    }
}
